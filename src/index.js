import "./assets/styles/styles.scss";
import "./index.scss";

AOS.init();
/*let dernierePositionScrollConnue = 0;
let ticking = false;

function faireQuelqueChose(positionScroll) {
  const expertises = document.querySelector(".expertises-content");
  console.log(expertises);
  expertises.classList.toggle('expertise-animation');
}

window.addEventListener('scroll', function(e){
  dernierePositionScrollConnue = window.scrollY;
  console.log(dernierePositionScrollConnue);
  if(!ticking) {
    window.requestAnimationFrame(function(){
      faireQuelqueChose(dernierePositionScrollConnue);
    })
  }
}) */